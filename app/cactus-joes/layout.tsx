export const metadata = {
  title: "The Voice of Cash × Cactus Joe's × The Crystal Garden",
  description: "AI Implementation Partner — Las Vegas",
  robots: "noindex, nofollow",
};

export default function CactusJoesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
